box: rocker/geospatial

build:
  steps:
    - script:
      name: Setup
      code: |
          install2.r googlesheets
    - script:
      name: Update scores
      code: |
          wget --quiet "http://www.stat.duke.edu/~cr173/Sta323_Sp17/hw/hw6_update_score.R"
          Rscript update_score.R $TEAM $URL $PP_URL
